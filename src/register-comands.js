const { REST, Routes } = require('discord.js');

const commands = [
    {
        name: 'hey',
        description: 'replies with hey',
    }
]

(async () => {
    try 

    } catch (error) {
        console.log(error)
    }
})